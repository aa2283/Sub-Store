name: Sub-Store 实时同步存档版

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_PAT }}
          fetch-depth: 0

      - name: 2. 启动服务 (挂载持久化配置)
        run: |
          mkdir -p sub-store-data
          docker run -d --name substore-svc \
            -v $(pwd)/sub-store-data:/opt/app/data \
            -p 3000:3000 -p 3001:3001 xream/sub-store
          sleep 15

      - name: 3. 下载并开启隧道
        run: |
          # 明确下载 cloudflared
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          
          # 启动隧道
          nohup ./cloudflared tunnel --url http://127.0.0.1:3000 --logfile backend.log 2>&1 &
          
          # 等待日志生成
          sleep 15
          if [ -f backend.log ]; then
            API_URL=$(grep -o 'https://[-0-9a-z]*\.trycloudflare\.com' backend.log | head -n 1)
            echo "========================================================="
            echo "🔗 前端管理入口: https://sub-store.vercel.app/?api=$API_URL"
            echo "========================================================="
          else
            echo "❌ 隧道启动失败，请检查 Docker 是否正常"
          fi

      - name: 4. 实时内网抓取与推送循环
        env:
          MY_TOKEN: ${{ secrets.MY_PAT }}
        run: |
          for i in {1..180}
          do
            echo ">>> 第 $i 次检查..."
            
            # --- 【核心修复：手动激活后端】 ---
            # 模拟浏览器访问首页和注入地址，强制后端加载配置
            curl -s -o /dev/null "http://127.0.0.1:3000"
            curl -s -o /dev/null "https://sub-store.vercel.app/?api=http://127.0.0.1:3000"
            sleep 2
            
            # 1. 探测订阅 (尝试获取订阅列表)
            RAW_SUBS=$(curl -s http://127.0.0.1:3000/api/utils/list?type=sub || echo "[]")
            SUB_NAME=$(echo "$RAW_SUBS" | jq -r '.[0].name' 2>/dev/null || echo "null")
            
            # 2. 如果订阅为空，尝试探测组合订阅 (Collection)
            if [ "$SUB_NAME" == "null" ] || [ -z "$SUB_NAME" ]; then
              RAW_COLLECTIONS=$(curl -s http://127.0.0.1:3000/api/utils/list?type=collection || echo "[]")
              SUB_NAME=$(echo "$RAW_COLLECTIONS" | jq -r '.[0].name' 2>/dev/null || echo "null")
              DL_URL="http://127.0.0.1:3000/download/collection/${SUB_NAME}?target=ClashMeta"
            else
              DL_URL="http://127.0.0.1:3000/download/${SUB_NAME}?target=ClashMeta"
            fi

            echo ">>> 探测结果: 目标名=[$SUB_NAME]"

            if [ "$SUB_NAME" != "null" ] && [ -n "$SUB_NAME" ]; then
              mkdir -p subs
              # 3. 执行抓取
              curl -s "$DL_URL" -o subs/new_all.yaml
              
              # 检查抓到的文件是不是空的或者报错网页
              if [ -s subs/new_all.yaml ] && ! grep -q "<html>" subs/new_all.yaml; then
                if [ ! -f "subs/all.yaml" ] || ! cmp -s "subs/new_all.yaml" "subs/all.yaml"; then
                  echo "📢 捕获成功且有变动，开始推送..."
                  cp subs/new_all.yaml subs/all.yaml
                  
                  # 存档 JSON 设置
                  docker cp substore-svc:/opt/app/data/sub-store.json ./sub-store-data/sub-store.json
                  
                  # 推送至当前仓库
                  git config --local user.email "actions@github.com"
                  git config --local user.name "GitHub Action"
                  git add .
                  git commit -m "自动存档: $(date +'%H:%M:%S')" || echo ""
                  git push origin main
                  
                  # 推送至 v2ray_configs
                  rm -rf target_repo
                  if git clone https://${MY_TOKEN}@github.com/aa2283/v2ray_configs.git target_repo; then
                    mkdir -p target_repo/subs
                    cp -f subs/all.yaml target_repo/subs/all.yaml
                    cd target_repo
                    git config --local user.email "actions@github.com"
                    git config --local user.name "GitHub Action"
                    git add .
                    git commit -m "实时更新: $(date +'%H:%M:%S')" || echo ""
                    git push origin main
                    cd ..
                    echo "🚀 推送完成！"
                  fi
                else
                  echo "😴 内容未变化。"
                fi
              else
                echo "⚠️ 抓取到的数据无效 (可能是后端未完全就绪)。"
              fi
            else
              echo "⚠️ 依然没发现订阅。请确保在网页里点击了[保存]按钮。"
            fi
            sleep 60
          done
