name: Sub-Store 实时同步存档版

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_PAT }}
          fetch-depth: 0

      - name: 2. 启动服务 (挂载持久化配置)
        run: |
          mkdir -p sub-store-data
          docker run -d --name substore-svc \
            -v $(pwd)/sub-store-data:/opt/app/data \
            -p 3000:3000 -p 3001:3001 xream/sub-store
          sleep 15

      - name: 3. 开启调试隧道 (仅供进入网页)
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          nohup ./cloudflared tunnel --url http://127.0.0.1:3000 --logfile backend.log 2>&1 &
          sleep 10
          API_URL=$(grep -o 'https://[-0-9a-z]*\.trycloudflare\.com' backend.log | head -n 1)
          echo "========================================================="
          echo "🛠️ 调试入口 (改完保存即可):"
          echo "👉 https://sub-store.vercel.app/?api=$API_URL"
          echo "========================================================="

      - name: 4. 实时内网抓取与推送循环
        env:
          MY_TOKEN: ${{ secrets.MY_PAT }} # 确保你已经在 Secrets 里设置了这个名字
        run: |
          for i in {1..180}
          do
            # 激活后端
            curl -s -o /dev/null "https://sub-store.vercel.app/?api=http://127.0.0.1:3000"
            
            # 获取订阅名称
            SUB_NAME=$(curl -s http://127.0.0.1:3000/api/utils/list?type=sub | jq -r '.[0].name')
            
            if [ "$SUB_NAME" != "null" ] && [ -n "$SUB_NAME" ]; then
              mkdir -p subs
              curl -s "http://127.0.0.1:3000/download/${SUB_NAME}?target=ClashMeta" -o subs/new_all.yaml
              
              # 检查是否有变动
              if [ ! -f "subs/all.yaml" ] || ! cmp -s "subs/new_all.yaml" "subs/all.yaml"; then
                echo "✅ 发现变动，正在同步至两个仓库..."
                cp subs/new_all.yaml subs/all.yaml
                
                # 备份 sub-store.json (存入当前 Sub-Store 仓库)
                docker cp substore-svc:/opt/app/data/sub-store.json ./sub-store-data/sub-store.json
                
                # 配置 Git 信息
                git config --local user.email "actions@github.com"
                git config --local user.name "GitHub Action"

                # --- 1. 推送配置备份到当前仓库 ---
                git add sub-store-data/sub-store.json subs/all.yaml
                git commit -m "自动存档: $(date +'%H:%M:%S')" || echo "无需提交"
                git push origin main || echo "当前仓库推送失败"

                # --- 2. 关键：跨仓库推送到 v2ray_configs ---
                # 使用你提供的 MY_PAT 进行鉴权
                rm -rf target_repo # 确保清理旧数据
                git clone https://${MY_TOKEN}@github.com/aa2283/v2ray_configs.git target_repo
                mkdir -p target_repo/subs
                cp -f subs/all.yaml target_repo/subs/all.yaml
                
                cd target_repo
                git config --local user.email "actions@github.com"
                git config --local user.name "GitHub Action"
                git add .
                git commit -m "内网实时同步: $(date +'%H:%M:%S')" || echo "无变化"
                git push origin main
                cd ..
                
                echo "🚀 全部仓库已同步完成！"
              fi
            fi
            sleep 60
          done
