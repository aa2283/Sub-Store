name: Sub-Store 终极自动化同步

on:
  workflow_dispatch: # 手动触发
  schedule:
    - cron: '0 */6 * * *' # 每 6 小时跑一次

jobs:
  run-sub-store:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_PAT }}
          fetch-depth: 0

      - name: 2. 启动 Sub-Store (持久化挂载)
        run: |
          # 确保备份目录存在
          mkdir -p sub-store-data
          mkdir -p subs
          
          # 启动容器：将仓库的备份目录挂载到容器内部数据目录
          docker run -d --name substore-svc \
            -v $(pwd)/sub-store-data:/opt/app/data \
            -p 3000:3000 \
            xream/sub-store
          
          echo "等待容器初始化..."
          sleep 20

      - name: 3. 开启隧道 (自指向访问模式)
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          
          # 启动隧道对准 3000
          nohup ./cloudflared tunnel --url http://127.0.0.1:3000 --logfile backend.log 2>&1 &
          
          sleep 15
          API_URL=$(grep -o 'https://[-0-9a-z]*\.trycloudflare\.com' backend.log | head -n 1)
          echo "========================================================="
          echo "✅ 纯本地管理入口 (无需 Vercel):"
          echo "👉 $API_URL/?api=$API_URL"
          echo "========================================================="

      - name: 4. 实时捕获与双仓库推送
        env:
          MY_TOKEN: ${{ secrets.MY_PAT }}
        run: |
          for i in {1..180} # 默认监控 3 小时
          do
            echo ">>> 第 $i 分钟检查循环..."

            # 【核心步骤】自动唤醒后端加载配置
            curl -s -o /dev/null "http://127.0.0.1:3000/?api=http://127.0.0.1:3000"
            sleep 3

            # 抓取 Clash 和 URI 两种格式 (假设订阅名为 sub)
            curl -s "http://127.0.0.1:3000/download/sub?target=ClashMeta" -o subs/new_clash.yaml
            curl -s "http://127.0.0.1:3000/download/sub?target=URI" -o subs/new_uri.txt

            # 检查抓取的数据是否有效
            if grep -q "proxies" subs/new_clash.yaml || grep -q "://" subs/new_uri.txt; then
              
              # 对比内容是否有变化 (Clash 或 URI 变了都推送)
              if [ ! -f "subs/all.yaml" ] || ! cmp -s "subs/new_clash.yaml" "subs/all.yaml" || ! cmp -s "subs/new_uri.txt" "subs/all.txt"; then
                echo "📢 检测到变动，开始执行同步推送..."
                
                cp subs/new_clash.yaml subs/all.yaml
                cp subs/new_uri.txt subs/all.txt
                
                # 从运行中的容器提取最新配置进行备份
                docker cp substore-svc:/opt/app/data/sub-store.json ./sub-store-data/sub-store.json

                # Git 身份配置
                git config --local user.email "actions@github.com"
                git config --local user.name "GitHub Action"

                # A. 推送到当前 Sub-Store 仓库 (存档 JSON 和订阅)
                git add sub-store-data/sub-store.json subs/all.yaml subs/all.txt
                git commit -m "自动存档: $(date +'%Y-%m-%d %H:%M:%S')" || echo ""
                git push origin main

                # B. 推送到外部 v2ray_configs 仓库 (供客户端使用)
                rm -rf target_repo
                if git clone https://${MY_TOKEN}@github.com/aa2283/v2ray_configs.git target_repo; then
                  mkdir -p target_repo/subs
                  cp -f subs/all.yaml target_repo/subs/all.yaml
                  cp -f subs/all.txt target_repo/subs/all.txt
                  
                  cd target_repo
                  git config --local user.email "actions@github.com"
                  git config --local user.name "GitHub Action"
                  git add .
                  git commit -m "节点实时更新: $(date +'%H:%M:%S')" || echo ""
                  git push origin main
                  cd ..
                  echo "🚀 v2ray_configs 同步成功！"
                fi
              else
                echo "😴 内容未变化，继续监听..."
              fi
            else
              echo "⚠️ 尚未获取到有效节点，请确保网页订阅名为 sub 且已点击保存。"
            fi
            
            sleep 60
          done
