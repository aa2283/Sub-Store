name: Sub-Store 同步 (带前端注入调试)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.MY_PAT }}

      - name: 2. 启动服务
        run: |
          mkdir -p sub-store-data
          docker run -d --name substore-svc \
            -v $(pwd)/sub-store-data:/opt/app/data \
            -p 3000:3000 -p 3001:3001 xream/sub-store
          sleep 15

      - name: 3. 开启隧道并生成注入链接
        run: |
          curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          nohup ./cloudflared tunnel --url http://127.0.0.1:3000 --logfile backend.log 2>&1 &
          sleep 15
          
          # 获取隧道的公网 API 地址
          API_URL=$(grep -o 'https://[-0-9a-z]*\.trycloudflare\.com' backend.log | head -n 1)
          
          echo "========================================================="
          echo "🔗 点击下方链接直接进入管理面板 (已自动注入后端):"
          echo "👉 https://sub-store.vercel.app/?api=$API_URL"
          echo "========================================================="
          echo "⚠️ 请在网页操作完成后，等待脚本自动执行后续推送。"

      - name: 4. 挂机等待 (预留时间给你在网页操作)
        run: |
          echo ">>> 正在挂机 15 分钟，请在此期间完成订阅设置..."
          sleep 900 

      - name: 5. 立即从 127.0.0.1 抓取并推送
        run: |
          mkdir -p subs
          # 使用 jq 自动获取你刚刚在网页里创建的订阅名
          SUB_NAME=$(curl -s http://127.0.0.1:3000/api/utils/list?type=sub | jq -r '.[0].name')
          
          if [ "$SUB_NAME" != "null" ]; then
            echo "发现订阅: $SUB_NAME，正在抓取..."
            curl -s "http://127.0.0.1:3000/download/${SUB_NAME}?target=ClashMeta" -o subs/all.yaml
            curl -s "http://127.0.0.1:3000/download/${SUB_NAME}?target=URI" -o subs/all.txt
          fi

      - name: 6. 推送至 v2ray_configs 仓库
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Action"
          git clone https://${{ secrets.MY_PAT }}@github.com/aa2283/v2ray_configs.git target_repo
          
          mkdir -p target_repo/subs
          cp -f subs/all.* target_repo/subs/
          
          cd target_repo
          git add .
          git commit -m "Web 手动配置后同步: $(date +'%Y-%m-%d %H:%M')" || exit 0
          git push origin main
